# Port the chat server listens on
PORT=3002

# MySQL connection (must match the Laravel server's DB)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=ninjasage
DB_USER=root
DB_PASS=

# Shared secret for the /admin/announce endpoint — must match CHAT_ADMIN_SECRET in Laravel .env
CHAT_ADMIN_SECRET=AdminSecret